[project]
name = "agentfeeds-hermes-plugin"
version = "0.1.0"
description = "Hermes plugin and skill for Agent Feeds"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"

[dependency-groups]
dev = [
  "pytest>=8.2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
