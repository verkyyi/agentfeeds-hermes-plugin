[project]
name = "agentfeeds-hermes-plugin"
version = "0.1.1"
description = "Hermes plugin for the Agent Feeds skill"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"

[dependency-groups]
dev = [
  "pytest>=8.2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
